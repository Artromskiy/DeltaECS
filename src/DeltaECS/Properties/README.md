# Assembly metadata

This folder contains assembly-level build metadata only. It defines no
consumer-facing ECS API. Public contracts are documented in the neighboring
API-role folders and the repository root README.
